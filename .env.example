# Copy to .env.local and fill in. On Vercel, set the same variables under
# Project → Settings → Environment Variables.

# Your Google Tag Manager container ID. Leave empty to run without GTM
# (events still go to window.dataLayer and the debug panel).
NEXT_PUBLIC_GTM_ID=
